// node_modules/@cloudflare/realtimekit-ui/dist/esm/date-b1f78dea.js
var differenceInMinutes = (oldDate, newDate) => {
  const diff = newDate.getTime() - oldDate.getTime();
  return Math.round(Math.abs(diff / 1e3 / 60));
};
var elapsedDuration = (oldDate, newDate) => {
  const minutes = differenceInMinutes(oldDate, newDate);
  if (minutes < 2) {
    return "just now";
  }
  if (minutes < 60) {
    return `${minutes}m ago`;
  }
  const hours = Math.round(minutes / 60);
  if (minutes < 90) {
    return `about ${hours}h ago`;
  }
  if (hours < 24) {
    return `${hours}h ago`;
  }
  const days = Math.round(hours / 24);
  if (days < 7) {
    return `${days}d ago`;
  }
  const weeks = Math.round(days / 7);
  return `${weeks}w ago`;
};
var formatDateTime = (date) => {
  return date.toDateString() + " " + date.toLocaleTimeString();
};

export {
  differenceInMinutes,
  elapsedDuration,
  formatDateTime
};
//# sourceMappingURL=chunk-V6N4A4IA.js.map
