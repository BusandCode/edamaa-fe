import {
  useGrid
} from "./chunk-HNETOGO6.js";
import {
  index
} from "./chunk-YMWEVWBA.js";
import {
  Render
} from "./chunk-QF5BB5L4.js";
import {
  SyncWithStore
} from "./chunk-DP3S5342.js";
import {
  Host,
  createDefaultConfig,
  defaultIconPack,
  getElement,
  h,
  registerInstance,
  useLanguage
} from "./chunk-M7NCH557.js";
import "./chunk-EX4CI3XV.js";

// node_modules/@cloudflare/realtimekit-ui/dist/esm/rtk-simple-grid.entry.js
var rtkSimpleGridCss = ":host{line-height:initial;font-family:var(--rtk-font-family, sans-serif);font-feature-settings:normal;font-variation-settings:normal}p{margin:var(--rtk-space-0, 0px);padding:var(--rtk-space-0, 0px)}:host{position:relative;display:block;height:100%;width:100%}";
var RtkSimpleGridStyle0 = rtkSimpleGridCss;
var __decorate = function(decorators, target, key, desc) {
  var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
  if (typeof Reflect === "object" && typeof Reflect.decorate === "function")
    r = Reflect.decorate(decorators, target, key, desc);
  else
    for (var i = decorators.length - 1; i >= 0; i--)
      if (d = decorators[i])
        r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
  return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var RtkSimpleGrid = class {
  constructor(hostRef) {
    registerInstance(this, hostRef);
    this.participants = [];
    this.aspectRatio = "16:9";
    this.gap = 8;
    this.config = createDefaultConfig();
    this.iconPack = defaultIconPack;
    this.t = useLanguage();
    this.dimensions = { width: 0, height: 0 };
    this.setHostDimensions = () => {
      const { clientWidth: width, clientHeight: height } = this.host;
      this.dimensions = { width, height };
    };
  }
  connectedCallback() {
    this.resizeObserver = new index(this.setHostDimensions);
    this.resizeObserver.observe(this.host);
    this.meetingChanged(this.meeting);
  }
  meetingChanged(meeting) {
    const meta = meeting === null || meeting === void 0 ? void 0 : meeting.meta;
    if (meta)
      this.mediaConnection = Object.assign({}, meta.mediaState);
  }
  disconnectedCallback() {
    var _a;
    (_a = this.resizeObserver) === null || _a === void 0 ? void 0 : _a.disconnect();
  }
  render() {
    if (!this.meeting)
      return null;
    const defaults = {
      meeting: this.meeting,
      config: this.config,
      states: this.states,
      size: this.size,
      iconPack: this.iconPack,
      t: this.t
    };
    const { width, height, getPosition } = useGrid({
      dimensions: this.dimensions,
      count: this.participants.length,
      aspectRatio: this.aspectRatio,
      gap: this.gap
    });
    return h(Host, null, h("slot", null, this.participants.map((participant, index2) => {
      const { top, left } = getPosition(index2);
      return h(Render, { element: "rtk-participant-tile", defaults, props: {
        participant,
        style: {
          position: "absolute",
          top: `${top}px`,
          left: `${left}px`,
          width: `${width}px`,
          height: `${height}px`
        },
        key: participant.id,
        "data-participant": participant.id,
        mediaConnection: this.mediaConnection
      }, childProps: { participant }, deepProps: true });
    })));
  }
  get host() {
    return getElement(this);
  }
  static get watchers() {
    return {
      "meeting": ["meetingChanged"]
    };
  }
};
__decorate([
  SyncWithStore()
], RtkSimpleGrid.prototype, "meeting", void 0);
__decorate([
  SyncWithStore()
], RtkSimpleGrid.prototype, "states", void 0);
__decorate([
  SyncWithStore()
], RtkSimpleGrid.prototype, "config", void 0);
__decorate([
  SyncWithStore()
], RtkSimpleGrid.prototype, "iconPack", void 0);
__decorate([
  SyncWithStore()
], RtkSimpleGrid.prototype, "t", void 0);
RtkSimpleGrid.style = RtkSimpleGridStyle0;
export {
  RtkSimpleGrid as rtk_simple_grid
};
//# sourceMappingURL=rtk-simple-grid.entry-INRP4M4O.js.map
